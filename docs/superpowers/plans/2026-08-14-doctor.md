# 一键 Doctor 体检 实现计划

日期:2026-08-14 · Spec: `docs/superpowers/specs/2026-08-14-doctor-design.md`

## Global Constraints

- 所有逻辑 home 参数化,TempHome 测试,不碰真实用户文件
- 薄命令层;中文提交;每任务一提交;TDD(先失败测试)
- i18n en/zh 同步;项目无弹窗惯例(内联面板)

## Task 1: doctor.rs 核心 — 类型 + local_checks(1–4 项)

**Files:** 新建 `src-tauri/src/doctor.rs`;`lib.rs` 注册 `pub mod doctor;`

- [x] 失败测试:`local_checks` 各项判定
  - path:init_status 两态不可注入 → path 检查直接读 `path_env::init_status()`,测试只验证映射函数 `path_check(status)`
  - deps:构造 AgentStatus(installed + missing_deps)→ error;全满足 → ok
  - orphans:agent_providers 含未安装 agent → warn(列 label);无 → ok
  - drift:TempHome 写 managed 配置再改动文件 → warn;一致 → ok(复用 `agent_sync_overview_at`)
- [x] 实现 `DoctorCheck/CheckStatus/DoctorReport` + `local_checks`
- [x] `cargo test doctor` 通过
- [x] Commit: `feat(doctor): 本地体检四项(PATH/依赖/孤儿绑定/漂移)`

## Task 2: 网络与后端检查 + doctor_run 命令

**Files:** `provider_test.rs`(抽 `test_endpoint`)、新建 `commands/doctor.rs`、`commands/mod.rs`、`lib.rs`

- [x] 重构:`provider_test` 命令体抽为 `pub async fn test_endpoint(...)`(纯移动,现有测试不动)
- [x] `network_checks`:启用 provider 的已配端点(anthropic/openai 各一)并发拨测(futures::join_all),全网络错误 → info 降级;无 provider → info
- [x] `backend_checks`:已安装后端 gateway_status Err → warn;无后端 → info
- [x] `commands/doctor.rs`:`doctor_run()` 读 CONFIG_LOCK 加载 config + `list_agent_status` + 拼三组;注册 invoke_handler
- [x] `cargo test` 全量通过
- [x] Commit: `feat(doctor): provider 拨测/网关检查 + doctor_run 命令`

## Task 3: 前端 — API + Agents 页体检面板 + i18n

**Files:** 新建 `src/lib/api/doctor.ts`;`src/routes/agents/+page.svelte`;`src/lib/i18n/{en,zh}.json`

- [x] `doctor.ts`:`doctorRun() -> DoctorReport` 类型封装(snake_case)
- [x] Agents 页头部「体检」按钮(与刷新并排)→ 顶部内联 glass-card 报告:status 图标着色(✓/⚠/✕/ℹ)、title/detail/hint、重新体检;体检中 spinner
- [x] i18n en/zh:`agents.doctor.*`(按钮、加载、各检查项 title/hint 文案键 = check id 驱动)
- [x] `npm run check` 0 错误
- [x] Commit: `feat(ui): Agents 页一键体检面板`

## Task 4: 收尾

- [x] `cargo test` + `npm run check` 全绿
- [x] ROADMAP #10 标 ✅;计划勾选
- [x] Commit: `docs: doctor 收尾`
